name: mldoctor
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.11.*
  - pip
  - cudatoolkit
  - pip:
    - torchvision
    - scikit-learn
    - tqdm
    - numpy
    - torch
    - scipy
    - pandas